# Sage Kaizen Architect Review

| Field | Value |
|---|---|
| **Date** | 2026-04-09 15:46 |
| **Mode** | full |
| **ARCHITECT model** | Qwen3.5-27B-Q6\_K (port 8012, CUDA0) |
| **Changed files** | 2 |
| **Scope chars** | 22,921 |

> This review was generated by the Sage Kaizen Architect Review Service.
> No changes were applied automatically. All suggestions require manual review.


# Sage Kaizen Architect Review Report

## Executive Summary
This review identifies a critical configuration mismatch in the VRAM budget and high-priority risks regarding session state scope and voice startup timing. While latency improvements are significant, the architectural alignment requires verification before finalizing the merge.

**Verdict:** NEEDS_WORK

## Critical & High Findings

### [CRITICAL] VRAM Budget Mismatch
**File**: `config/brains/brains.yaml` (line 25)
**Issue**: The configuration header and VRAM budget comments specify **Q8_0** (~8.10 GB), but the `model` path points to **Q6_K** (~6.0 GB). This creates a discrepancy between expected resource usage and actual deployment.
**Action**: Verify if Q6_K is intentional. If Q8_0 is required for quality, update the path. If Q6_K is intentional, update the VRAM budget comments to reflect the lower usage.

### [HIGH] Session State Persistence Ambiguity
**File**: `src/_zmq_handlers.py` (line 443)
**Issue**: The assignment `session_id = None` may be ineffective if `session_id` is a local variable rather than a shared state (nonlocal or class attribute). This risks the "spurious barge-in" bug returning on subsequent turns.
**Action**: Verify variable scope. If local, declare `nonlocal session_id` or change to `self.session_id = None` if within a class method.

### [HIGH] Voice Startup Decoupling Risk
**File**: `src/voice_pipeline.py` (line 202)
**Issue**: The `voice_ready` signal is now sent *before* the greeting audio plays (removed `drain()` wait). This introduces a dependency on the Main App to handle greeting playback timing.
**Action**: Confirm the Main App's "Ready" logic waits for the `voice_ready` ZMQ message before triggering the greeting to avoid audio glitches.

## Medium Findings

### [MEDIUM] TTS Buffering Logic Alignment
**File**: `src/_zmq_handlers.py` (line 123)
**Issue**: Accumulating sentences `< 4` words aligns with the Observability-First Pattern but requires validation that this threshold prevents "choppy" audio without introducing excessive latency.
**Action**: Monitor audio smoothness during the next user session; adjust threshold if latency feels too high.

### [MEDIUM] Thread Count vs. CPU Cores
**File**: `config/brains/brains.yaml` (line 42)
**Issue**: `threads: 10` is set on a Ryzen 9 9950X3D (16 Physical Cores). While safe, it may under-utilize physical cores during RAG-heavy turns.
**Action**: Consider increasing `threads` to `12` or `14` to improve prefill speed without starving the OS.

### [MEDIUM] KV Cache Effectiveness
**File**: `config/brains/brains.yaml` (line 58)
**Issue**: `cache_ram: 512` is set for the FAST brain.
**Action**: Ensure this 512 MiB RAM cache is sufficient to store the system prompt + RAG context to effectively reduce Time-To-First-Token (TTFT) on subsequent turns.

## Code Quality Findings

### Dead Code
- **Status**: Clean. No unused functions or variables detected above 80% confidence.

### Code Smells
- **[LOW] Magic Number** (`src/_zmq_handlers.py:123`)
  - The literal `4` in `if len(sentence.split()) < 4:` is hardcoded.
  - **Recommendation**: Define `MIN_SENTENCE_WORDS = 4` at the module header.

### Optimization Opportunities
- **[LOW] List Allocation** (`src/_zmq_handlers.py:123`)
  - `sentence.split()` creates a new list in the tight token stream loop.
  - **Recommendation**: If profiling confirms overhead, use a generator or space count to avoid full list allocation.

### Best Practice Violations
- **Status**: Clean. No missing type annotations or bare `except` clauses detected.

### Performance Antipatterns
- **Status**: Clean. Removal of `await self._player.drain()` successfully reduces blocking time in startup.

## Low / Style Findings
- **Greeting Ownership**: Greeting moved to Main App aligns with Dual-Brain Pattern (`src/voice_pipeline.py:205`).
- **Model Alias Drift**: `alias` says Q8_0, `model` path says Q6_K (`config/brains/brains.yaml:25`).
- **Thread Utilization**: 10 threads on 16-core CPU is conservative (`config/brains/brains.yaml:42`).

## Performance & Latency Notes
- **Startup Latency**: Significantly reduced by removing `drain()` wait in `voice_pipeline.py`. `voice_ready` signal fires immediately upon initialization.
- **TTS Fragmentation**: Buffering sentences `< 4` words reduces CPU/GPU overhead on the TTS worker and improves perceived audio smoothness.
- **SearXNG Caching**: Valkey cache configured to reduce retrieval latency for repeated web search queries.
- **KV Cache**: 512 MiB RAM cache configured to store system prompt + RAG context, reducing TTFT on subsequent turns.

## Documentation Updates Required
- **CLAUDE.md (Section 2)**: Update Voice Pipeline section to note greeting is sent via ZMQ token bus by Main App after Q5, Q6, and Voice are ready.
- **CLAUDE.md (Section 5)**: Add invariant: "Voice session_id is cleared after TTS subscriber completes to prevent spurious barge-in events."
- **MEMORY.md**: Add project decisions regarding greeting handoff and session_id clearing logic.
- **01-ARCHITECTURE.md (Section 3)**: Add `voice_pipeline.py` and `_zmq_handlers.py` to Module Boundaries table.
- **02-ARCH-PATTERNS.md (Section 6)**: Note that Voice readiness signal is consolidated with Q5/Q6 readiness.

## Patch Suggestions

### Patch: Align Model Path with VRAM Budget
**File**: `config/brains/brains.yaml`
**Priority**: CRITICAL
```diff
  # BEFORE
  model: "E:/Qwen2.5-Omni-7B-GGUF/Qwen2.5-Omni-7B-Q6_K.gguf"
  
  # AFTER (If Q8_0 is intended)
  model: "E:/Qwen2.5-Omni-7B-GGUF/Qwen2.5-Omni-7B-Q8_0.gguf"
```
*(Note: If Q6_K is intentional, update the comment header and VRAM budget to reflect Q6_K weights.)*

### Patch: Ensure Session ID Scope Persistence
**File**: `src/_zmq_handlers.py`
**Priority**: HIGH
```diff
  # APPROXIMATE
  # Check if session_id is defined outside this function scope.
  # If it is a local variable, this assignment is ineffective for the next call.
  # Suggested fix if it's a class method:
- session_id = None
+ self.session_id = None
```

### Patch: Optimize Thread Count for Ryzen 9950X3D
**File**: `config/brains/brains.yaml`
**Priority**: LOW
```diff
  # BEFORE
  threads: 10
  
  # AFTER
  threads: 12
```

## Verdict
**NEEDS_WORK** — Critical VRAM configuration mismatch and High-priority session state scope ambiguity require verification before merge.