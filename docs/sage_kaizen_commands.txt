python -m streamlit run ui_streamlit.py

python -m streamlit run ui_streamlit_server.py


python.exe -m rag_v1.ingest.folder_ingest


python rag_diagnostics.py

#Ingest local media files
# Run ingest (from project root):
python -m rag_v1.media.media_ingest --root "F:/Projects/sage_kaizen_ai/media_samples"

# For GPU-accelerated ingest (faster):
python -m rag_v1.media.media_ingest --root "F:/path/to/your/media" --device cuda:0

# Re-runs are safe — already-ingested files are skipped:
python -m rag_v1.media.media_ingest --root "F:/path/to/your/media"
  

Export Zim Files Command

# All 4 configured ZIM jobs:
python -m rag_v1.ingest.zim_dump

# Single ZIM file:
python -m rag_v1.ingest.zim_dump --zim "C:\path\to\file.zim" --out "I:\llm_data\output_dir"

python -m rag_v1.ingest.zim_dump --zim "C:\Users\Alquin\AppData\Roaming\kiwix-desktop\wikisource_en_all_maxi_2026-02.zim" --out "I:\llm_data\wikisource_maxi_2026_02"

python -m rag_v1.ingest.zim_dump --zim "C:\Users\Alquin\AppData\Roaming\kiwix-desktop\wikipedia_en_all_maxi_2025-08.zim" --out "I:\llm_data\wikipedia_maxi_2025_08"

python -m rag_v1.ingest.zim_dump --zim "C:\Users\Alquin\AppData\Roaming\kiwix-desktop\wikipedia_en_all_nopic_2025-12.zim" --out "I:\llm_data\wikipedia_nopic_2025_08"
  
  
# Create Wikipedia Embeddings

# Ingest wiki_samples 
python -m rag_v1.wiki.wiki_ingest


The --root flag still exists as an optional one-time override — useful if you want to test against a different dump without editing YAML:
python -m rag_v1.wiki.wiki_ingest --root "F:\Projects\sage_kaizen_ai\wiki_samples"


Verification commands

# Config sanity
python -c "from rag_v1.wiki.wiki_embed_config import load_wiki_embed_config; c = load_wiki_embed_config(); print(c.text_batch, c.image_batch)"
# Expected: 128 32

# Smoke test (single letter bucket — no embed service needed to check startup logic)
python -m rag_v1.wiki.wiki_ingest --root "I:/llm_data/wikipedia_maxi_2025_08/A"
# Expect: "Worker A started", "Worker B started", progress every 30s, "Ingest complete."

# Resume safety test (run same root twice)
# Second run: skipped=N  written=0  errors=0






  
  